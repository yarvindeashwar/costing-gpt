name: Azure Static Web App CI/CD

on:
  push:
    branches:
      - master
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - master

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: "65e6cffdb90c68f0b2e64a6f79a0323d53d013e3cc4c98b9f1bff4e76b70ff0706-a9208fce-b485-49c7-a31d-1881641d4f0d00010290e1984900"
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "apps/portal"
          api_location: ""
          output_location: ".next"
          app_build_command: "npm run build"
          api_build_command: ""

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: "65e6cffdb90c68f0b2e64a6f79a0323d53d013e3cc4c98b9f1bff4e76b70ff0706-a9208fce-b485-49c7-a31d-1881641d4f0d00010290e1984900"
          action: "close"
